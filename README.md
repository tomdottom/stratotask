# stratotastic

A _Cloud Task_ scheduler and dispatcher.

# Setup

    pipenv sync --dev

# Tests

    pipenv run pytest src/tests/
    pipenv run mypy src/

# PoC demo

    export PYTHONPATH=src/
    python -m stratotask
